<?xml version="1.0" encoding="utf-8"?>
<!-- The frame the coordinate comes from: the design tints its card and outlines it. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/ds_good_surface" />
    <stroke android:width="1.5dp" android:color="@color/ds_good_edge" />
    <corners android:radius="20dp" />
</shape>
